# Sample environment variables for CrownWynn backend
# Copy this to .env and fill in secure values for local development.

# Django
DJANGO_SECRET_KEY=replace-with-a-secure-random-string
DJANGO_DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Deployment notes:
# - `ALLOWED_HOSTS` must contain hostnames only (no scheme). Example for Render:
#     ALLOWED_HOSTS=crownwynn.onrender.com
# - `CORS_ALLOWED_ORIGINS` and `CSRF_TRUSTED_ORIGINS` must include the scheme (https://)
#   and are used to allow requests from your frontend (Vercel). Example:
#     CORS_ALLOWED_ORIGINS=https://crownwynn.vercel.app
#     CSRF_TRUSTED_ORIGINS=https://crownwynn.vercel.app

# Database (Postgres example)
# Format: postgres://user:password@host:port/dbname
DATABASE_URL=postgres://crownuser:crownpass@localhost:5432/crownwynn

# Security cookies
AUTH_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_SECURE=False

# Example production values (do not commit real secrets):
# ALLOWED_HOSTS=crownwynn.onrender.com
# CORS_ALLOWED_ORIGINS=https://crownwynn.vercel.app
# CSRF_TRUSTED_ORIGINS=https://crownwynn.vercel.app
